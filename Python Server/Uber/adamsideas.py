#THIS IS PRETTY FUCKING BAD RN AND THE SYNTAX IS ALL WRONG BUT ITS JUST MY IDEA

import glob

def main(){
	obj = find()
	
	if(obj != null):
		request(obj)
		
	time.sleep(60000)
}

#list dir contents and find files that need reuqesting. run this every 1 minute
def find(){ 

files = glob.glob("/path/to/files/*.xml")

#loop through files
for x in range (0, len(glob.glob)):
	if (x - time) < 30: #if desired time - current time is less then an arbitray number (30?)
		return glob.glob[x]
	else:
		i++
}

def request(){
	uber api request with info from object
}

